---
interface Props {
  /** 'tip' | 'note' | 'warning' */
  type?: 'tip' | 'note' | 'warning'
}

const { type = 'tip' } = Astro.props
---

<aside class:list={['callout', `callout--${type}`]}>
  <p class="callout__type">{type}</p>
  <div class="callout__body">
    <slot />
  </div>
</aside>
